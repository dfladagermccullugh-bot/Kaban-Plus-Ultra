# This directory is otherwise generated by `npx cap add ios` on a machine
# with Xcode. We pre-create it so `apps/mobile/ios/ExportOptions.plist` has
# a home before the first `cap add ios` run. The release script's
# preflight check still catches a missing Xcode workspace.
